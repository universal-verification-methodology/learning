# Module 06 examples — Verilator trace

Track A (Verilator tool literacy). Browser lab: `verilator-trace` (**Shipped**).

## Prompts

1. Restate in one sentence what `--trace` enables vs what `tfp->dump` does in the C++ TB.
2. Sketch when you would pick `--trace-fst` over default VCD.
3. Optional: peek at [`../learn_verilator_iverilog/`](../learn_verilator_iverilog/) for a matching Verilator example.

## Stretch

Redo the same idea in the browser lab starter challenges (`platform/tools/verilator-trace/`).
