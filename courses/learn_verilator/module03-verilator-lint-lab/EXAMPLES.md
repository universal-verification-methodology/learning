# Module 03 examples — Verilator lint

Track A (Verilator tool literacy). Browser lab: `verilator-lint-lab` (**Shipped**).

## Prompts

1. Restate in one sentence what `-Wall` does and name two warning ideas (e.g. UNUSED, WIDTH).
2. Sketch when you would use `-Werror` vs `-Wno-<CODE>` — and why fixing RTL is usually better than silence.
3. Optional: peek at [`../learn_verilator_iverilog/`](../learn_verilator_iverilog/) for a matching Verilator example.

## Stretch

Redo the same idea in the browser lab starter challenges (`platform/tools/verilator-lint-lab/`).
