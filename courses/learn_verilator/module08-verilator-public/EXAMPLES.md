# Module 08 examples — Verilator public

Track A (Verilator tool literacy). Browser lab: `verilator-public` (**Shipped**).

## Prompts

1. Restate in one sentence why `/*verilator public*/` matters for a C++ TB peeking at an internal signal.
2. Sketch the difference between `public` and `public_flat`, and when a top port needs neither.
3. Optional: peek at [`../learn_verilator_iverilog/`](../learn_verilator_iverilog/) for a matching Verilator example.

## Stretch

Redo the same idea in the browser lab starter challenges (`platform/tools/verilator-public/`).
